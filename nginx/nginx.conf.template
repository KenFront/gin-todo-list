
server {
    listen 80;
    
    location / {
        proxy_pass_request_headers      on;
        proxy_pass http://web;
    }

    location /api/ {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass_request_headers      on;
        proxy_pass http://server;
    }
}
